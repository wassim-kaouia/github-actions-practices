name: My first workflow
run-name: ${{ github.actor }} is learning github actions

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello wassim ! this is a nice push ! keep going ..."
